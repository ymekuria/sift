machine: 
  services:
    - docker

dependencies:
  override:
    - sudo pip install --upgrade docker-compose==1.3.3

test:
  override:
    - cd app; npm test

deployment: 
  hub:
    branch: master
    commands:
      - $DEPLOY